{
  "description": "A fix/ branch that changes a PROHIBITED file, in a repository whose trunk is called 'trunk'. Neither 'origin/main' nor 'main' resolves, so Get-ChangedFiles returns an empty list and the Lite lane's guards grade it: an empty accusation is never proved. The branch touches a dependency manifest, which this lane exists to forbid.",
  "defaultBranch": "trunk",
  "commits": [
    {
      "branch": "trunk",
      "message": "init",
      "write": {
        "README.md": "fixture kit\n"
      }
    },
    {
      "branch": "fix/thing",
      "message": "fix: bump a dependency",
      "write": {
        "package.json": "{ \"dependencies\": { \"left-pad\": \"1.3.0\" } }\n"
      }
    }
  ],
  "checkout": "fix/thing"
}
